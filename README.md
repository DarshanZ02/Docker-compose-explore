# Docker-compose-explore
In this repository we write docker file for node js application and write the docker compose file for services and up all the container database, frontend and postgres admin UI with stateful container not stateless
